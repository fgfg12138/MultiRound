# PRD：主持人私密消息系统（Whisper System）

> 项目：MultiRound — Electron + React + TypeScript 桌面应用
> 作者：Alice（Product Manager）
> 版本：v0.1（简单 PRD + 竞品分析）
> 日期：2025-07-18

---

## 1. 产品目标

**在圆桌讨论流程中，为主持人提供与单个或多个角色之间的双向私密通信通道，主讨论流对其完全不可见。**

---

## 2. 用户故事

### 2.1 1:1 私信

- **As a 主持人**, I want to 在讨论暂停时给某个角色单独发送一条私密信息，So that 我可以暗中提供线索、下达指令或传递场景信息，而不影响其他角色的认知。
- **As a 角色（AI）**, I want to 收到主持人私信后可以回复，且回复仅主持人可见，So that 我可以私下汇报观察、提出策略建议或确认指令。

### 2.2 私密群组

- **As a 主持人**, I want to 拉一个包含多个角色的私密群组，So that 我可以与特定小组成员同时沟通，实现阵营内协调或秘密任务协作。
- **As a 群组成员（AI）**, I want to 在群组内看到其他成员的发言并按顺序回复，So that 我可以与同阵营角色私下讨论策略。

### 2.3 暂停态操作

- **As a 主持人**, I want to 在讨论暂停时自由编辑和发送私信，并管理群组，So that 我可以在不干扰主流程的情况下完成私密操作，然后在准备好后恢复讨论。

---

## 3. 需求池（Priority）

### P0 — 必须实现（MVP）

| ID | 需求 | 说明 |
|----|------|------|
| W-01 | **私密消息数据结构** | 新增 `WhisperMessage` 类型，包含：id, roundTableId, senderId, recipientId (单角色) 或 groupId (群组), type ('1:1' \| 'group'), content, timestamp, status ('sent' \| 'read') |
| W-02 | **私密群组数据结构** | 新增 `WhisperGroup` 类型，包含：id, roundTableId, name, hostId (主持人默认群主), memberIds (角色 ID 数组), speakOrder ('sequential' \| 'free' \| 'host-assigned'), createdAt |
| W-03 | **暂停态私信面板 UI** | 讨论暂停时，右侧出现「私密消息」面板（或弹窗），显示私信列表和输入区 |
| W-04 | **1:1 私信发送** | 主持人选择单个角色 → 输入内容 → 发送；消息显示在私信列表中，仅主持人和目标角色可见 |
| W-05 | **角色回复私信** | 角色在收到私信后，自动生成 AI 回复，通过 LLM 流式生成并发送回主持人（主讨论流程不感知） |
| W-06 | **角色视角能看到私信** | 角色在发言时，其 Prompt 中注入收到的但未回复的私信上下文，使角色的发言能回应主持人私信 |
| W-07 | **私信持久化** | 私信和群组数据随 RoundTable 一起存储/加载（独立文件 `{filename}_whispers.json`） |

### P1 — 重要（后续迭代）

| ID | 需求 | 说明 |
|----|------|------|
| W-08 | **私密群组创建/管理** | 主持人在暂停面板中创建群组（选择群名 + 成员），支持编辑成员、删除群组 |
| W-09 | **群组内发送消息** | 向群组发消息，群内所有可见 |
| W-10 | **群组消息顺序控制** | 群组聊天支持 sequential / free / host-assigned 顺序（决定群组内 AI 回复的顺序） |
| W-11 | **群组内 AI 自动回复** | 群组消息发送后，群内角色依次自动回复（按群组的 speakOrder），流式生成 |

### P2 — Nice to Have

| ID | 需求 | 说明 |
|----|------|------|
| W-12 | **消息已读状态** | 角色读取私信后更新 `status` 为 'read'，UI 显示已读标记 |
| W-13 | **群组消息历史** | 群组聊天显示历史记录，可滚动 |
| W-14 | **主讨论期间也可用** | 不暂停的情况下也支持发送私信（可选，低优先级） |

---

## 4. UI 设计稿（文字描述 + 布局）

### 4.1 整体布局

讨论页面在暂停时，右侧原本的「讨论信息」面板下方/替换为**私密消息面板**。采用 tab 切换：

```
┌─────────────────────────────────────────────────┐
│  左侧角色列表  │     主讨论流（暂停遮罩）         │  右侧面板          │
│                 │                                 │  ┌──────────────┐  │
│  ● 主持人       │  消息气泡 ...                   │  │ 讨论信息     │  │
│  ● 角色A        │                                 │  │ 主题: ...    │  │
│  ● 角色B        │      [暂停中]                    │  │ 轮数: ...    │  │
│  ● 角色C        │                                 │  ├──────────────┤  │
│                 │                                 │  │ [私密消息] ✦ │  │
│                 │  [继续] [停止生成]               │  │ ──────────── │  │
│                 │                                 │  │ ▼ 1:1 对话   │  │
│                 │                                 │  │   ├ 角色A     │  │
│                 │                                 │  │   └ 角色C     │  │
│                 │                                 │  │ ▼ 群组       │  │
│                 │                                 │  │   └ 暗中联盟  │  │
│                 │                                 │  │              │  │
│                 │                                 │  │ [输入框...]   │  │
│                 │                                 │  │ [发送]       │  │
└─────────────────────────────────────────────────┘
```

### 4.2 私密消息面板组件结构

```
私密消息面板
├── Tab 栏: [1:1 私信] [群组聊天]
│
├── 1:1 私信 Tab
│   ├── 联系人列表（角色选择）
│   │   └── 每个角色项显示：头像色块 + 名称 + 未读数 Badge
│   ├── 消息列表（选中角色后的聊天记录）
│   │   └── 消息气泡（主持人发送/角色回复，两侧区分）
│   └── 底部输入区
│       ├── 文本输入框（多行）
│       └── [发送] 按钮
│
├── 群组聊天 Tab
│   ├── 群组列表
│   │   ├── 每个群组项：群名 + 成员摘要 + 未读数 Badge
│   │   └── [+ 新建群组] 按钮
│   ├── 消息列表（选中群组后的聊天记录）
│   │   └── 消息气泡（按发送者分组显示）
│   ├── 群组信息栏（收起状态）
│   │   ├── 群组名
│   │   ├── 成员列表（可移除）
│   │   ├── 发言顺序模式选择器
│   │   └── [邀请角色] 按钮
│   └── 底部输入区
│       ├── 文本输入框（多行）
│       └── [发送] 按钮
│
└── 创建群组弹窗
    ├── 群组名输入
    ├── 成员多选（角色列表 checkbox）
    └── [创建] [取消]
```

### 4.3 视觉设计要点

- **私密消息气泡**使用特殊样式（虚线边框 + 锁图标 🔒），以区别于主讨论流气泡
- **角色回复**的消息气泡带有 AI 生成标识
- **未读私信**在右侧面板标签上显示红色 Badge
- **所有私密消息**在 UI 中明确标注「仅你和 XX 可见」

---

## 5. 业务规则

### 5.1 消息可见性规则

| 场景 | 主持人可见 | 角色A可见 | 角色B可见 | 其他角色 |
|------|-----------|----------|----------|---------|
| 主持人 → 角色A 私信 | ✅ | ✅ | ❌ | ❌ |
| 角色A 回复主持人 | ✅ | ✅ | ❌ | ❌ |
| 群组「X」消息（成员：A, B） | ✅ | ✅ | ✅ | ❌ |
| 主讨论流消息 | ✅ | ✅ | ✅ | ✅ |

### 5.2 暂停/继续时的行为

| 事件 | 行为 |
|------|------|
| 讨论**暂停** | 右侧面板展开私密消息功能，主持人可自由编辑/发送私信、管理群组 |
| 讨论**继续** | 右侧面板收起为 tab，禁止发送新私信（UI 输入框 disabled），已发送待角色回复的私信在后台继续处理 |
| 讨论**停止** | 所有未读私信标记为已读，数据持久化 |
| 主持人**发送私信后立即继续** | 私信已发送，等待角色在下一轮主讨论或后台自动回复 |

### 5.3 与主讨论流程的关系

| 方面 | 规则 |
|------|------|
| **Prompt 注入** | 角色发言的 Prompt 中追加「你收到的主持人私信（私密，不得泄露）」段落，包含未回复的私信内容 |
| **角色私信回复时机** | 角色在「记忆中更新」阶段自动生成私信回复（不单独调用 LLM，而是在记忆更新后附带生成私信回复） |
| **记忆更新** | 私信内容**不写入**公开记忆（publicMemory），只写入**私有记忆**（privateMemory） |
| **持久化隔离** | 主讨论消息存 `_messages.json`，私信/群组存 `_whispers.json`，互不影响 |

### 5.4 群组发言顺序（P1）

| 模式 | 行为 |
|------|------|
| sequential | 按群组成员加入顺序依次自动回复 |
| free | 随机 shuffle 后自动回复 |
| host-assigned | 主持人指定下一个回复的角色（在群组界面选择「指定 XX 回复」） |

---

## 6. 竞品分析

### 6.1 相关竞品/参考产品

| 产品 | 相关功能 | 对我们的启发 |
|------|---------|------------|
| **ChatGPT 自定义 GPT** | 可以在对话中切换不同自定义指令 | 角色可以有不同的「私密视角」，但缺乏 IRL 角色之间的私密通道 |
| **Character.AI** | 与 AI 角色 1:1 对话，支持群聊（Group Chat） | 群聊模式可参考，但无主持人私密通道 |
| **Dify / Coze 工作流** | Agent 间通过工作流节点通信 | 可参考其「私密变量传递」模式，但偏后台非 UI |
| **RPG 游戏叙事工具（如 TaleSpire、D&D Beyond）** | DM（主持人）可私下给玩家发送信息，支持秘密掷骰 | **最直接的参考**：DM 私信 → 秘密信息传递 → 角色回应 |
| **Slack / Discord 私信 + 私密频道** | 1:1 DM + 私密群组 | UI/UX 模式参考：联系人列表、未读标记、消息分组 |

### 6.2 差异化定位

| 维度 | 竞品 | MultiRound Whisper |
|------|------|--------------------|
| 消息可见性 | 通常全局可见或角色完全隔离 | **混合模式**：主讨论公开 + 子讨论私密 |
| 暂停态操作 | 不感知讨论流程 | **暂停态专属 UI**，适配讨论流程控制 |
| 群组发言顺序 | 自由发言为主 | **支持三种顺序模式**（sequential/free/host-assigned） |
| 私信注入 LLM | 无 | **私信内容自动注入角色 Prompt**，让 AI 角色理解并回应 |

---

## 7. 待确认问题（Open Questions）

1. **角色私信回复的触发时机**：角色是在「记忆更新阶段」同步生成私信回复，还是在主讨论流每轮发言前单独触发一次 LLM 调用？前者复用现有记忆更新流程但可能延迟，后者更及时但增加 LLM 调用次数。**建议**：在记忆更新阶段附带生成私信回复，减少额外 LLM 开销。

2. **群组消息的 AI 回复顺序和并发**：群组消息发送后，群内角色是否需要**按顺序**生成回复（sequential 模式），还是可以并发？如果 sequential，是否会阻塞其他非群组角色的主讨论流？**建议**：群组 AI 回复在后台按序执行，不阻塞主讨论流；完成后 IPC 推送消息到渲染进程。

3. **私信角色回复中断场景**：如果角色在回复私信的过程中讨论被停止或角色被移除，如何处理未完成的私信回复？**建议**：使用 AbortController 统一管理，停止时清理所有未完成的 LLM 调用。

---

## 8. 技术实现要点（给架构师参考）

### 8.1 新增类型定义（`types.ts`）

```typescript
export interface WhisperMessage {
  id: string;
  roundTableId: string;
  senderId: string | 'host';
  recipientId?: string;       // 1:1 时的目标角色 ID
  groupId?: string;            // 群组消息时关联的群组 ID
  type: '1:1' | 'group';
  content: string;
  timestamp: number;
  status: 'sent' | 'read';
}

export interface WhisperGroup {
  id: string;
  roundTableId: string;
  name: string;
  hostId: string | 'host';
  memberIds: string[];         // 角色 ID 列表
  speakOrder: SpeakOrder;      // 'sequential' | 'free' | 'host-assigned'
  createdAt: number;
}
```

### 8.2 新增 IPC 通道

| 通道 | 方向 | 用途 |
|------|------|------|
| `whisper:send` | renderer → main | 主持人发送私信 |
| `whisper:send-group` | renderer → main | 主持人发送群组消息 |
| `whisper:create-group` | renderer → main | 创建私密群组 |
| `whisper:group-reply` | main → renderer | 角色回复私信推送 |
| `whisper:load` | renderer → main | 加载私信历史 |

### 8.3 存储

- 私信数据独立存储：`{filename}_whispers.json`
- 文件结构：`{ whispers: WhisperMessage[], groups: WhisperGroup[] }`
- 随 RoundTable 的 load/save 流程自动处理

---

*PRD 结束 — 提交给架构师进行技术设计。*
